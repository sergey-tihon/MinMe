<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="MinMe.Avalonia.Views.PowerPointView"
             DataContext="{Binding PowerPoint}">
    <UserControl.Styles>
        <Style Selector="Button">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="2"/>
        </Style>
    </UserControl.Styles>
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button Command="{Binding OpenCommand}">Open</Button>
            <Button Command="{Binding OptimizeCommand}">Optimize</Button>
            <Button Command="{Binding PublishCommand}">Publish Slides</Button>
        </StackPanel>
        <Border Padding="16">
            <TextBlock Text="{Binding FileName}"></TextBlock>
            <!-- <TextBlock Text="{Binding FileContentInfo.FileSize}"></TextBlock> -->
        </Border>
    </DockPanel>
</UserControl>
